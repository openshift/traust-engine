# Unit tests carry deliberately fake, token-shaped strings that are never live.
# Keep forge secret scanning on real leaks.
paths-ignore:
  - "tests/**"
  - "src/traust_engine/data/secure-code-audit/gitleaks-rules/**"
  - "src/traust_engine/data/secure-code-audit/opengrep-rules/**"
